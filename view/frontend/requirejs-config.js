var config = {
    paths: {
        /**
         * Allow loading the TrustCaptcha widget via RequireJS/AMD from the module's local copy.
         * RequireJS will append ".js" automatically, so keep the path without ".js".
         */
        'trustcaptcha-umd': 'TrustComponent_TrustCaptchaMagento2/js/trustcaptcha-3.0.1.umd.min'
    }
};
