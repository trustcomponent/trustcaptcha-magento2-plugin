var config = {
    paths: {
        /**
         * Allow loading the TrustCaptcha widget via RequireJS/AMD.
         * RequireJS will append ".js" automatically, so keep the path without ".js".
         */
        'trustcaptcha-umd': 'https://cdn.trustcomponent.com/trustcaptcha/2.1.x/trustcaptcha.umd.min'
    }
};
